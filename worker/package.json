{
  "name": "notion-expense-bot",
  "version": "1.0.0",
  "description": "Telegram bot to log expenses to Notion using Cloudflare Worker",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.100.0"
  }
}
